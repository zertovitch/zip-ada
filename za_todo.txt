- move comments to .ads in zip.compress
- reduce: better comment, say it is optimal
- profiling
    uza: 34% of run time used by System.Exp_Uns.Exp_Unsigned
    
- reduce: partial (<2**n) follower table can be worth, see pkzip090 (if only 1 or 2 times used, a single follower is a loss of bits)
- reduce meth: perhaps mimic pkzip092: if input_size_known and then input_size < 2KB then Shrink
- reduce: increase lz cache to...
